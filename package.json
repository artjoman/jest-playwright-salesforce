{
  "devDependencies": {
    "@types/jest": "^27.0.3",
    "dotenv": "^10.0.0",
    "jest": "^27.4.4",
    "jest-playwright-preset": "^1.7.0",
    "playwright": "^1.17.1",
    "ts-jest": "^27.1.1",
    "typescript": "^4.5.3"
  },
  "scripts": {
    "test": "cp .env.development .env && jest",
    "test.watch": "cp .env.development .env && jest --watchAll",
    "test-prod": "cp .env.production .env && jest"
  }
}
